buildscript {
    repositories {
        mavenCentral()
        maven { url 'http://download.crashlytics.com/maven' }

    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.7.+'
        classpath 'com.crashlytics.tools.gradle:crashlytics-gradle:1.+'

    }
}
apply plugin: 'android'
apply plugin: 'crashlytics'

repositories {
    mavenCentral()
    maven { url 'http://download.crashlytics.com/maven' }

}

android {
	compileSdkVersion 19
    buildToolsVersion "19.0.1"

    defaultConfig {
        minSdkVersion 8
        targetSdkVersion 19
		versionCode 13
        versionName "1.12"
    }

    signingConfigs {
        release {
            storeFile file("/home/izzy/Dropbox/Android/KeyStore/izzy-release-key.keystore")
            storePassword "1zzyb0ngarc0n"
            keyAlias "izzy-signature"
            keyPassword "1zzyb0ngarc0n"
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
			runProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
	
	lintOptions {
        abortOnError false
    }
}

dependencies {
    compile 'com.google.code.gson:gson:2.2.+'
    compile 'org.jsoup:jsoup:1.7.+'
	compile 'com.android.support:support-v4:19.0.+'
	compile 'com.android.support:appcompat-v7:19.0.+'
	compile 'com.github.chrisbanes.actionbarpulltorefresh:extra-abc:0.9.+'
	compile 'com.astuetz:pagerslidingtabstrip:1.0.+'
	compile('de.keyboardsurfer.android.widget:crouton:1.8.+') {
		exclude group: 'com.google.android', module: 'support-v4'
	}
    compile 'se.emilsjolander:stickylistheaders:2.1.5'
    compile 'com.nostra13.universalimageloader:universal-image-loader:1.9.1'
    compile 'com.crashlytics.android:crashlytics:1.+'
    compile project(":ActionBar-PullToRefresh-development:library")
    compile project(":ActionBar-PullToRefresh-development:extras:actionbarcompat")
}


